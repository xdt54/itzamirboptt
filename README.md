# 🤖 Telegram Bot - سیستم بازی استراتژیک

یک بات تلگرام کامل با قابلیت‌های:
- 💰 سیستم اقتصادی
- ⚔️ سیستم جنگ
- ⛏️ استخراج منابع
- 🛡️ پدافند و تسلیحات
- 🔐 پنل ادمین پیشرفته
- 📊 لاگینگ به گروه تلگرام
- 💾 بکاپ خودکار

---

## 📋 فهرست مطالب

- [ویژگی‌ها](#ویژگیها)
- [نصب و راه‌اندازی](#نصب-و-راهاندازی)
- [ساختار پروژه](#ساختار-پروژه)
- [پیکربندی](#پیکربندی)
- [استفاده](#استفاده)
- [مستندات](#مستندات)

---

## ✨ ویژگی‌ها

### 🎮 سیستم بازی

#### 💰 اقتصاد
- کوین (سکه) و نقره
- خرید و فروش آیتم‌ها
- تبدیل سکه/نقره
- بانک برای سپرده‌گذاری

#### ⛏️ استخراج
- ماین طلا
- ماین آهن
- ماین نقره
- سیستم خودکار استخراج

#### 🛒 فروشگاه
- خرید سلاح (8 نوع)
- خرید پدافند (8 نوع)
- خرید موشک (8 نوع)
- مدیریت زرادخانه

#### ⚔️ سیستم جنگ
- حمله با موشک‌های مختلف
- محاسبه قدرت حمله
- سیستم دفاعی
- گزارش نبرد

### 🔐 پنل مدیریت

#### 👥 مدیریت کاربران
- مشاهده لیست کاربران
- جستجوی کاربر
- ویرایش موجودی
- بن/آنبن کاربر
- ارسال پیام به کاربر
- آمار کاربران

#### 💰 مدیریت اقتصاد
- تنظیم قیمت‌ها
- اعطای کوین/نقره
- مشاهده بزرگترین‌ها
- تنظیم سرعت ماین
- محدودیت خرید

#### 🎉 رویدادها
- ایجاد رویداد
- تخفیف‌ها
- مسابقات
- جوایز

#### 📊 آمار و گزارش
- آمار عمومی
- آمار فروش
- آمار جنگ
- کاربران فعال

#### ⚙️ تنظیمات
- مدیریت ادمین‌ها (اضافه/حذف)
- تنظیم گروه لاگ
- حالت تعمیر و نگهداری
- پیکربندی سیستم

#### 💾 بکاپ
- بکاپ خودکار هر 6 ساعت
- بکاپ دستی
- نگهداری 10 بکاپ آخر
- ارسال به گروه لاگ

### 📊 سیستم لاگینگ

#### 7 Topic مختلف:
- ⚙️ **سیستم**: راه‌اندازی، خطاها
- 👥 **کاربران**: ثبت‌نام، فعالیت
- 💰 **اقتصاد**: خرید، فروش
- ⚔️ **جنگ**: نبردها، نتایج
- 🔐 **ادمین**: عملیات مدیریتی
- 💾 **بکاپ**: فایل‌های بکاپ
- 🛡️ **امنیت**: تلاش‌های مشکوک

#### ویژگی‌ها:
- ذخیره‌سازی Topic ID ها
- پایداری بعد از ریستارت
- ارسال خودکار لاگ‌ها
- فرمت زیبا با Emoji

---

## 🚀 نصب و راه‌اندازی

### پیش‌نیازها

```bash
Python 3.9+
pip
Telegram Bot Token
```

### نصب

1. **کلون پروژه:**
```bash
git clone <repository-url>
cd "itz amir bot"
```

2. **نصب وابستگی‌ها:**
```bash
pip install -r requirements.txt
```

3. **پیکربندی:**
```bash
# کپی فایل .env نمونه
cp .env.example .env
```

4. **ویرایش `.env`:**
```env
BOT_TOKEN=your_bot_token_here
```

5. **تنظیم سوپر ادمین:**
```python
# config/admin_config.py
SUPER_ADMIN_IDS = [
    123456789,  # User ID شما
]
```

6. **راه‌اندازی:**
```bash
python main.py
```

---

## 📁 ساختار پروژه

```
itz amir bot/
│
├── main.py                 # فایل اصلی بات
├── requirements.txt        # وابستگی‌های Python
├── .env                    # تنظیمات محرمانه
│
├── config/                 # پیکربندی‌ها
│   ├── settings.py         # تنظیمات عمومی
│   └── admin_config.py     # تنظیمات ادمین
│
├── database/               # پایگاه داده
│   ├── db.py              # مدیریت دیتابیس اصلی
│   ├── admin_db.py        # دیتابیس ادمین‌ها
│   └── users.db           # فایل SQLite
│
├── handlers/               # Handler های بات
│   ├── main.py            # دستورات اصلی
│   ├── router.py          # مسیریابی پیام‌ها
│   ├── shop.py            # فروشگاه
│   ├── mine.py            # استخراج
│   ├── war.py             # سیستم جنگ
│   ├── bank.py            # بانک
│   └── admin.py           # پنل ادمین
│
├── keyboards/              # کیبوردهای شیشه‌ای
│   └── main_keyboard.py   # کیبورد اصلی
│
├── utils/                  # ابزارها
│   ├── logger.py          # لاگر
│   ├── log_manager.py     # مدیریت لاگ به تلگرام
│   ├── backup_manager.py  # مدیریت بکاپ
│   └── mining_loop.py     # حلقه استخراج
│
├── data/                   # داده‌های ذخیره شده
│   ├── log_topics.json    # Topic ID ها
│   └── admin_settings.json # تنظیمات ادمین
│
└── backups/                # فایل‌های بکاپ
    └── backup_*.db
```

---

## ⚙️ پیکربندی

### 1. تنظیمات اصلی

```python
# config/settings.py
BOT_TOKEN = os.getenv("BOT_TOKEN")
```

### 2. تنظیمات ادمین

```python
# config/admin_config.py
SUPER_ADMIN_IDS = [123456789]
BACKUP_INTERVAL = 6 * 3600  # 6 ساعت

PERMISSIONS = {
    "super_admin": [...],
    "admin": [...],
    "moderator": [...]
}
```

### 3. گروه لاگ

از طریق بات:
```
/admin → ⚙️ تنظیمات → 📍 تنظیم گروه لاگ
```

یا دستی:
```python
# در بات:
admin_db.set_log_group(-1001234567890)
```

---

## 📖 استفاده

### برای کاربران

#### شروع
```
/start
```

#### منوی اصلی
- 💰 **پروفایل**: مشاهده موجودی
- 🛒 **فروشگاه**: خرید آیتم
- ⛏️ **ماین**: استخراج منابع
- ⚔️ **جنگ**: حمله به دیگران
- 🏦 **بانک**: سپرده‌گذاری

#### جنگ
در پیوی:
```
حمله @username نام_موشک
```

در گروه:
```
حمله نام_موشک
(Reply به هدف)
```

### برای ادمین‌ها

```
/admin
```

منوی ادمین:
- 👥 مدیریت کاربران
- 💰 مدیریت اقتصاد
- 🎉 رویدادها
- 📊 آمار و گزارش
- ⚙️ تنظیمات
- 💾 مدیریت بکاپ

---

## 📚 مستندات

- [راهنمای تنظیم ادمین](ADMIN_SETUP.md)
- [راهنمای مدیریت ادمین‌ها](ADMIN_MANAGEMENT.md)

---

## 🔧 توسعه

### ساختار دیتابیس

```sql
-- کاربران
CREATE TABLE users (
    user_id INTEGER PRIMARY KEY,
    username TEXT,
    coin INTEGER DEFAULT 0,
    silver INTEGER DEFAULT 0,
    gold_level INTEGER DEFAULT 0,
    iron_level INTEGER DEFAULT 0,
    silver_level INTEGER DEFAULT 0,
    ...
);

-- ادمین‌ها
CREATE TABLE admins (
    user_id INTEGER PRIMARY KEY,
    username TEXT,
    role TEXT,
    added_by INTEGER,
    added_at TIMESTAMP,
    is_active BOOLEAN
);
```

### اضافه کردن قابلیت جدید

1. ایجاد handler در `handlers/`
2. ثبت در `main.py`
3. اضافه کردن به منو در `keyboards/`

---

## 🐛 مشکلات رایج

### بات پاسخ نمی‌دهد
```bash
# بررسی لاگ‌ها
python main.py
```

### دیتابیس خطا می‌دهد
```bash
# پاک کردن و بازسازی
rm users.db
python main.py
```

### لاگ ارسال نمی‌شود
- بررسی کنید بات Admin گروه است
- Topics باید فعال باشد
- Group ID صحیح باشد

---

## 🔐 امنیت

### سوپر ادمین
- تنها از کد قابل تنظیم
- غیرقابل تغییر از بات
- دسترسی کامل به تمام امکانات

### ادمین‌ها
- از طریق بات قابل اضافه/حذف
- دسترسی محدود
- لاگ تمام عملیات

---

## 📝 لایسنس

این پروژه تحت لایسنس MIT منتشر شده است.

---

## 👨‍💻 توسعه‌دهنده

ساخته شده توسط GitHub Copilot

---

## 🙏 تشکر

از کتابخانه‌های زیر استفاده شده:
- [python-telegram-bot](https://python-telegram-bot.org/)
- [SQLite3](https://www.sqlite.org/)
- [python-dotenv](https://pypi.org/project/python-dotenv/)

---

## 📞 پشتیبانی

برای مشکلات و پیشنهادات:
1. Issue در GitHub باز کنید
2. مستندات را مطالعه کنید
3. لاگ‌های console را بررسی کنید
