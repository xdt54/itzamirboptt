# 🔐 راهنمای مدیریت ادمین‌ها

## نحوه استفاده از سیستم مدیریت ادمین

### 1️⃣ تنظیم سوپر ادمین (اولین بار)

سوپر ادمین از طریق کد تنظیم می‌شود و قابل تغییر از بات نیست:

```python
# config/admin_config.py
SUPER_ADMIN_IDS = [
    123456789,  # User ID سوپر ادمین اصلی
    987654321,  # User ID سوپر ادمین دوم (اختیاری)
]
```

**نحوه دریافت User ID:**
1. به بات [@userinfobot](https://t.me/userinfobot) پیام بدهید
2. User ID شما نمایش داده می‌شود

---

### 2️⃣ راه‌اندازی گروه لاگ

#### الف) ایجاد گروه

1. یک گروه جدید در تلگرام بسازید
2. **Topics را فعال کنید:**
   - Group Settings → Topics → Enable

3. **بات را ادمین کنید با این دسترسی‌ها:**
   - ✅ Send Messages
   - ✅ Send Media
   - ✅ Manage Topics

#### ب) دریافت Group ID

**روش 1: استفاده از @userinfobot**
```
1. بات @userinfobot را به گروه اضافه کنید
2. یک پیام بفرستید
3. Group ID نمایش داده می‌شود (مثال: -1001234567890)
```

**روش 2: استفاده از @getidsbot**
```
1. بات @getidsbot را به گروه اضافه کنید
2. دستور /start را بزنید
3. Group ID نمایش داده می‌شود
```

#### ج) تنظیم از طریق بات

1. دستور `/admin` را به بات بزنید
2. **⚙️ تنظیمات** را انتخاب کنید
3. **📍 تنظیم گروه لاگ** را کلیک کنید
4. Group ID را ارسال کنید (مثال: `-1001234567890`)
5. بات به صورت خودکار 7 Topic ایجاد می‌کند:
   - ⚙️ سیستم
   - 👥 کاربران
   - 💰 اقتصاد
   - ⚔️ جنگ
   - 🔐 ادمین
   - 💾 بکاپ
   - 🛡️ امنیت

---

### 3️⃣ اضافه کردن ادمین

**فقط سوپر ادمین می‌تواند ادمین اضافه کند**

#### مراحل:
1. دستور `/admin` را بزنید
2. **⚙️ تنظیمات** → **👥 مدیریت ادمین‌ها**
3. **➕ اضافه کردن ادمین** را کلیک کنید
4. User ID کاربر را ارسال کنید
5. ادمین جدید با نقش **Admin** اضافه می‌شود

#### نقش‌های مختلف:

| نقش | توضیحات | دسترسی‌ها |
|-----|---------|-----------|
| 👑 **Super Admin** | مدیر اصلی (فقط از کد) | همه دسترسی‌ها + مدیریت ادمین‌ها |
| ⭐ **Admin** | مدیر | مدیریت کاربران، اقتصاد، رویدادها |
| 👤 **Moderator** | مدیر محدود | فقط مشاهده آمار |

---

### 4️⃣ حذف ادمین

1. `/admin` → **⚙️ تنظیمات** → **👥 مدیریت ادمین‌ها**
2. روی ادمینی که می‌خواهید حذف کنید کلیک کنید
3. تایید کنید

**نکته:** سوپر ادمین‌ها قابل حذف نیستند!

---

### 5️⃣ مشاهده لیست ادمین‌ها

```
/admin → ⚙️ تنظیمات → 👥 مدیریت ادمین‌ها
```

در این صفحه می‌بینید:
- تعداد کل ادمین‌ها
- User ID و نقش هر ادمین
- زمان اضافه شدن
- توسط چه کسی اضافه شده

---

### 6️⃣ بکاپ خودکار

سیستم بکاپ به صورت خودکار هر **6 ساعت** یک بار:
- پایگاه داده را بکاپ می‌گیرد
- فایل را به گروه لاگ (Topic بکاپ) ارسال می‌کند
- 10 بکاپ آخر نگهداری می‌شود

**مدیریت دستی بکاپ:**
```
/admin → 💾 مدیریت بکاپ
```

---

## 🔒 امنیت

### سوپر ادمین
- ✅ تنها کسی که می‌تواند ادمین اضافه/حذف کند
- ✅ تنها کسی که می‌تواند گروه لاگ را تغییر دهد
- ✅ User ID از کد تنظیم می‌شود (غیرقابل تغییر از بات)

### ادمین‌های عادی
- ⚠️ قابل اضافه/حذف توسط سوپر ادمین
- ⚠️ نمی‌توانند ادمین دیگری اضافه کنند
- ⚠️ نمی‌توانند گروه لاگ را تغییر دهند

### لاگ تمام عملیات
تمام عملیات ادمین‌ها ثبت می‌شود:
- اضافه/حذف ادمین
- تغییر تنظیمات
- تغییر اقتصاد
- مدیریت کاربران

---

## 📁 فایل‌های مهم

```
database/
  ├── users.db              # دیتابیس اصلی کاربران
  └── admin_db.py           # کلاس مدیریت ادمین‌ها

data/
  ├── log_topics.json       # Topic ID های ذخیره شده
  └── admin_settings.json   # تنظیمات (گروه لاگ، حالت نگهداری)

config/
  └── admin_config.py       # سوپر ادمین‌های هاردکد شده

handlers/
  └── admin.py              # تمام handler های پنل ادمین

utils/
  ├── log_manager.py        # مدیریت لاگ به گروه
  └── backup_manager.py     # بکاپ خودکار
```

---

## 🐛 عیب‌یابی

### گروه لاگ کار نمی‌کند
```
✅ بررسی کنید بات Admin گروه است
✅ بررسی کنید Topics فعال است
✅ بررسی کنید Group ID صحیح است (با - شروع شود)
✅ لاگ‌ها را چک کنید: console بات
```

### نمی‌توانم ادمین اضافه کنم
```
✅ بررسی کنید شما Super Admin هستید
✅ User ID صحیح وارد شده باشد
✅ بررسی کنید آن User ID قبلاً ادمین نشده باشد
```

### بکاپ ارسال نمی‌شود
```
✅ گروه لاگ تنظیم شده باشد
✅ Topic "💾 بکاپ" ایجاد شده باشد
✅ بات دسترسی ارسال فایل داشته باشد
```

---

## 🚀 شروع سریع

1. **User ID خود را پیدا کنید:**
   ```
   @userinfobot → /start
   ```

2. **در کد سوپر ادمین شوید:**
   ```python
   # config/admin_config.py
   SUPER_ADMIN_IDS = [YOUR_USER_ID]
   ```

3. **بات را راه‌اندازی کنید:**
   ```bash
   python main.py
   ```

4. **گروه لاگ بسازید و تنظیم کنید:**
   ```
   /admin → ⚙️ تنظیمات → 📍 تنظیم گروه لاگ
   ```

5. **ادمین‌های دیگر را اضافه کنید:**
   ```
   /admin → ⚙️ تنظیمات → 👥 مدیریت ادمین‌ها → ➕
   ```

✅ **تمام!** حالا سیستم ادمین شما آماده است.

---

## 📞 پشتیبانی

در صورت بروز مشکل:
1. لاگ‌های console را بررسی کنید
2. فایل `ADMIN_SETUP.md` را مطالعه کنید
3. بررسی کنید تمام دسترسی‌های بات صحیح است
